node_modules/
.next/
out/
build/
dist/
coverage/

.env
.env.local
.env.*.local
!.env.example

*.tsbuildinfo
next-env.d.ts

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Machine-specific: `npx skills add` writes absolute-path symlinks here that
# point into one developer's home directory. The skills themselves live in
# .agents/skills/ and are committed; re-run the installer to relink.
.claude/skills/

# Real people's email addresses. Kept out of git even though the file
# is only a list of inputs to scripts/add-team.ts.
team.txt
