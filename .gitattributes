# Normalise to LF in the repository. Dev machine is Windows, CI runs Ubuntu —
# without this, line endings show up as whole-file diffs between them.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
